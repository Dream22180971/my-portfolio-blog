import type { BlogPost } from "@/lib/blog-data";

const post = {
  slug: "ai-generated-test-cases-human-review",
  title: "AI生成了100条测试用例，我是如何人工审核并筛出有效用例的",
  date: "2026-08-09",
  readTime: "12 分钟",
  excerpt: "AI写出100条用例不难，难的是判断它们是否通俗、可执行、优先级合理，并且真正覆盖了测试点。这是我的人工审核和脚本校验流程。",
  tags: ["AI测试", "测试用例", "人工审核", "测试工程", "质量保证"],
  content: `> AI可以提高测试用例的产出速度，但“产出得快”和“产出得准”是两件事。

## 写在前面

现在把一份需求文档交给AI，让它生成几十条甚至上百条测试用例，已经不是什么难事。

真正让我在意的问题是：

- 这些用例，测试人员能不能一眼看懂？
- 拿到用例以后，能不能直接执行？
- P0、P1、P2的优先级划分准确吗？
- 输出的数量比例合理吗？
- 需求里的每个测试点真的都覆盖了吗？

如果这些问题没有解决，AI只是把“写用例”变成了“批量制造待整理的文字”。生成100条，然后让测试工程师花半天删除重复内容，这不叫提效，只是把工作从编写变成了清洗。

所以我的做法不是让AI生成完就结束，而是增加两道关：

1. **人工审核**：判断业务是否正确、风险是否合理。
2. **脚本校验**：检查格式、比例、字段和测试点覆盖是否符合规则。

这篇文章就用一次“AI生成100条测试用例”的过程，把这套方法讲清楚。

---

## 一、AI生成100条用例之后，我不会马上导入用例库

假设需求是一个常见的登录功能，包括：

- 账号密码登录
- 图形验证码
- 连续输错密码后锁定
- 找回密码
- 登录成功后保持会话

AI很快就能生成100条用例。数量看起来很漂亮，但打开一看，通常会出现几类问题：

- “验证用户登录功能是否正常”——看得懂，但不能直接执行。
- “输入错误密码时验证登录失败”——方向正确，但没有说明错误几次、提示什么、账号状态如何变化。
- “验证指纹登录成功”——需求里根本没有指纹登录，是AI自己补出来的。
- “验证密码为空”和“验证未输入密码”——本质是重复用例。
- 所有用例都标记为P0——数量很多，但没有优先级。

这就是AI生成测试用例最容易制造的假象：**看起来覆盖很广，实际上大量内容不准确、不可执行、不可管理。**

因此，AI生成的第一版在我的流程里只能叫“候选用例”，不能直接叫“正式用例”。

---

## 二、第一关：用例标题必须让人一眼看懂

我对用例标题有一个很朴素的要求：看到标题，就应该知道在什么条件下，系统应该发生什么。

我习惯使用这样的格式：

> **当……时，系统应该……**

例如：

| 不推荐的标题 | 调整后的标题 |
|---|---|
| 验证登录失败 | 当用户输入错误密码时，系统应提示账号或密码错误 |
| 验证账号锁定 | 当用户连续5次输入错误密码时，系统应锁定账号30分钟 |
| 验证验证码 | 当用户输入过期验证码时，系统应提示验证码已失效 |
| 验证会话 | 当已登录用户刷新页面时，系统应保持登录状态 |
| 验证退出 | 当用户退出登录后再次访问个人中心时，系统应跳转到登录页 |

这种写法有三个好处：

1. **通俗易懂**：产品、开发和测试看到的是同一个业务场景。
2. **便于执行**：标题已经包含测试条件和预期方向。
3. **便于检查**：脚本可以判断标题是否包含“当”和“时”等结构。

但标题格式统一，不代表用例就一定合格。

例如：

> 当用户登录时，系统应该正常工作。

形式上符合“当……时”，实际上仍然无法执行。“正常工作”到底是什么？跳转到首页、返回Token，还是展示用户昵称？

所以标题检查还要继续追问：

- 条件是否具体？
- 操作对象是否明确？
- 预期结果是否能够观察？
- 执行人员能否根据标题快速理解测试目的？

我的标准不是句式漂亮，而是**标题能够准确表达一条可执行的测试意图。**

---

## 三、第二关：用例必须真正可执行

一条正式测试用例至少应该包含：

- 用例标题
- 需求或测试点来源
- 前置条件
- 测试步骤
- 测试数据
- 预期结果
- 优先级

缺少这些字段，不一定完全不能测试，但会把大量判断留给执行人员。

例如AI输出：

> 当用户连续输入错误密码时，系统应锁定账号。

这条用例方向没错，但还缺少关键内容：

- 连续输入多少次？
- 统计周期是多久？
- 锁定多长时间？
- 锁定后再次输入正确密码会怎样？
- 页面提示什么？
- 后台是否产生安全日志？

补充以后，它才是一条真正可执行的用例：

**标题：** 当用户在10分钟内连续5次输入错误密码时，系统应锁定账号30分钟。

**前置条件：** 测试账号处于正常状态，已设置正确密码。

**测试步骤：** 连续5次提交错误密码，第6次提交正确密码。

**预期结果：** 前4次提示账号或密码错误；第5次提示账号已锁定；第6次即使密码正确也不能登录；安全日志记录锁定事件。

这时，执行人员不需要猜测需求，也不需要临时决定“怎样才算通过”。

---

## 四、第三关：检查用例优先级是否准确

AI很容易把优先级写得过高。原因也很简单：它缺少真实业务背景，不知道哪个功能每天有10万人使用，哪个功能一年只会触发一次。

我的划分方式是：

| 优先级 | 判断标准 | 登录场景示例 |
|---|---|---|
| P0 | 核心链路完全不可用，或可能造成严重资金、安全、数据问题 | 正确账号密码无法登录；普通用户绕过认证进入后台 |
| P1 | 重要功能异常，存在明确用户影响，但有替代路径 | 验证码刷新失败；账号锁定时间不符合规则 |
| P2 | 边缘场景、体验问题或低频兼容问题 | 错误提示文案不统一；极小分辨率下提示换行 |

审核优先级时，我会问三个问题：

1. 失败后影响多少用户？
2. 是否影响资金、权限、隐私或核心数据？
3. 用户有没有替代路径？

优先级不是看用例写得复杂不复杂，而是看失败以后会造成多大损失。

### 按比例输出，但不要机械凑比例

为了防止AI把所有用例都标成P0，我会给生成结果设置一个参考比例：

- P0：约10%到20%
- P1：约50%到60%
- P2：约20%到30%

这个比例不是绝对标准，而是一个异常检测信号。

如果100条登录用例里有80条P0，通常说明AI没有真正理解优先级；如果一个支付核心链路完全没有P0，同样说明结果有问题。

脚本可以检查比例是否偏离预期，但最后是否调整，仍然要由测试人员结合业务风险判断。

---

## 五、第四关：100条用例是否真的覆盖了所有测试点

用例多，不代表覆盖完整。

AI可能围绕“密码错误”生成20条相似用例，却漏掉账号锁定后的解锁、会话失效、接口重复提交等关键测试点。

所以在生成用例之前，我会先把需求拆成测试点清单：

| 测试点ID | 测试点 | 最低覆盖要求 |
|---|---|---|
| LOGIN-01 | 正确账号密码登录 | 至少1条正常用例 |
| LOGIN-02 | 错误密码处理 | 错误提示、剩余次数 |
| LOGIN-03 | 连续失败锁定 | 阈值、锁定时间、锁定后登录 |
| LOGIN-04 | 验证码 | 正确、错误、过期、刷新 |
| LOGIN-05 | 会话管理 | 刷新、超时、退出后失效 |
| LOGIN-06 | 找回密码 | 链接有效期、重复使用、账号校验 |

然后要求AI输出的每条用例都必须关联一个或多个测试点ID。

这样就能回答两个关键问题：

- 每一条用例到底在验证什么？
- 每一个测试点是否至少有一条有效用例覆盖？

脚本可以统计：

- 没有关联测试点的孤立用例
- 没有任何用例覆盖的遗漏测试点
- 被大量重复覆盖的测试点
- P0测试点是否有对应的P0用例

这比只统计“总共生成了多少条用例”更有意义。

---

## 六、把常见检查写成脚本，先做机器Check

人工应该判断业务和风险，不应该把时间浪费在机械检查上。

因此，我会把稳定、重复、规则明确的检查写成脚本。AI生成用例以后，先执行一次check，再把通过基础检查的候选用例交给人工审核。

脚本主要检查五类问题：

### 1. 标题格式

- 是否使用“当……时，系统应……”结构
- 是否包含明确条件和可观察结果
- 是否出现“正常”“正确处理”“符合预期”等模糊词

### 2. 必填字段

- 标题、前置条件、步骤、预期结果是否为空
- 是否填写优先级
- 是否关联需求ID或测试点ID

### 3. 优先级比例

- P0、P1、P2占比是否严重偏离参考范围
- 是否存在核心测试点没有P0用例
- 是否把普通文案或UI问题错误标记为P0

### 4. 测试点覆盖

- 所有测试点是否至少被一条有效用例覆盖
- 是否存在没有测试点来源的用例
- 是否在同一个测试点上生成大量高度相似的用例

### 5. 可执行性

- 步骤中是否有明确操作
- 预期结果是否可以观察或断言
- 测试数据是否具体
- 是否引用了需求中不存在的字段、页面或功能

脚本输出的不是简单的“通过”或“失败”，而是一份问题清单：

| 用例ID | 检查结果 | 问题 |
|---|---|---|
| TC-001 | 通过 | 无 |
| TC-012 | 需修改 | 标题预期结果不明确 |
| TC-027 | 需修改 | 未关联测试点ID |
| TC-043 | 退回 | 引用了需求中不存在的指纹登录 |
| TC-068 | 重复 | 与TC-031场景高度相似 |

脚本负责发现规则问题，人工负责决定怎样修改。

---

## 七、脚本Check以后，人工到底审核什么

机器检查通过，并不代表用例一定正确。

有些问题只有真正理解业务的人才能判断：

- 这个场景在线上真的会发生吗？
- 失败后造成的影响是否足以标记为P0？
- 需求本身有没有矛盾或遗漏？
- AI给出的预期结果是否符合真实业务规则？
- 是否缺少历史上出现过的高风险问题？

我会把人工审核结果分为三类：

| 审核结果 | 含义 | 后续动作 |
|---|---|---|
| 可以直接使用 | 业务正确、步骤完整、预期明确 | 加入正式用例库 |
| 修改后使用 | 主体正确，但存在缺失或表达问题 | 修改后再次Check |
| 不能使用 | AI虚构、重复、方向错误或无法执行 | 删除或退回重新生成 |

这里我不会要求审核人再计算一套复杂的0到1置信度。

对日常功能测试来说，“可以直接使用、修改后使用、不能使用”已经足够清楚，也更容易在团队里落地。

---

## 八、100条用例最后可能只保留60条，这很正常

假设AI最初生成100条用例，经过脚本和人工审核后得到：

- 删除重复用例：18条
- 删除AI虚构用例：8条
- 合并相似用例：14条
- 修改后保留：35条
- 直接保留：25条
- 人工补充AI遗漏的高风险用例：8条

最终正式用例库里可能是68条，而不是100条。

这不代表AI生成失败。

AI的价值是快速提供一个覆盖面更大的候选集合，测试工程师的价值是把候选集合变成可靠的质量资产。

真正应该关注的不是“AI一次生成了多少条”，而是：

- 最终有效用例率是多少？
- 人工修改了多少条？
- 漏掉了多少关键测试点？
- 脚本提前拦截了多少格式和覆盖问题？
- 这批用例最终发现了多少有效缺陷？

---

## 九、我的完整工作流

最后把流程收拢一下：

1. 产品需求先拆成结构化测试点，并给每个测试点分配ID。
2. AI根据需求和测试点生成候选用例。
3. 用例标题统一使用“当……时，系统应……”的通俗格式。
4. 每条用例必须包含前置条件、步骤、数据、预期和优先级。
5. 脚本check标题、必填字段、优先级比例、重复度和测试点覆盖。
6. 测试工程师审核业务正确性、真实风险和可执行性。
7. 修改后的用例再次运行check。
8. 审核通过后才进入正式用例库和自动化回归范围。

这套流程里，AI不是最终决策者，脚本也不是最终决策者。

AI负责扩展思路，脚本负责执行规则，测试工程师负责判断质量。

---

## 写在最后

AI生成测试用例真正有价值的地方，不是把原来两小时的写作用例压缩到十分钟，而是让测试人员把更多精力放在风险、覆盖和判断上。

但前提是，我们不能把AI输出直接当成结果。

如果没有标题规范、可执行性检查、优先级规则、测试点映射和人工审核，生成得越多，后面的清理成本可能越高。

我更愿意把AI生成的内容称为“候选用例”。

候选用例经过脚本check和人工审核以后，才会成为真正可以执行、可以维护、可以回归的测试用例。

**AI负责把网撒得更大，测试工程师负责确保网里留下的东西真正有价值。**

---

*本文是“肖恩的博客”系列文章之一，记录AI在真实测试工作流中的落地实践。*`,
} satisfies BlogPost;

export default post;
